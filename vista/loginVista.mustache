

<div class="login-card text-center">
    <div class="title">
        <img src="/assets/img/triviaGo.png" alt="Logo" style="width:50%" />
        <p>Demostrá cuánto sabés</p>
    </div>

    <form method="POST" action="/user/login">
        {{#error}}
            <div class="alert alert-danger" role="alert">{{error}}</div>
        {{/error}}
        {{#success}}
            <div class="alert alert-success" role="alert">{{success}}</div>
        {{/success}}
        <div class="mb-3 text-start">
            <label for="usuario" class="form-label">Usuario</label>
            <input type="text" class="form-control" id="usuario" name="usuario" required>
        </div>

        <div class="mb-3 text-start">
            <label for="contrasena" class="form-label">Contraseña</label>
            <input type="password" class="form-control" id="contrasena" name="contrasena" required>
        </div>

        <button type="submit" class="btn btn-custom">Entrar</button>
    </form>
    <div class="container mt-3">
        <p>¿No tienes una cuenta? <a href="/user/register">Regístrate</a>.</p>
    </div>
    <div class="container">
        <p>¿Olvidaste tu contraseña? <a href="/user/olvidarContraseña">Recuperala aquí</a>.</p>
    <div>
        <p>¿Queres validar tu cuenta? <a href="/user/validacionUno">Hacélo aquí</a>.</p>
</div>
    </div>
</div>
